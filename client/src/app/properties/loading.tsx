import { Loading } from '../../components/common/Loading';

export default function PropertiesLoading() {
    return (
        <main className="page-shell">
            <Loading label="Đang tải danh sách bất động sản..." />
        </main>
    );
}
